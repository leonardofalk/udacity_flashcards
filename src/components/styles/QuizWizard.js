import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  pagination: {
    marginBottom: 20,
  },

  paginationText: {
    fontWeight: 'bold',
  },

  scoreMessage: {
    textAlign: 'center',
    fontWeight: 'bold',
  },

  scoreContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
